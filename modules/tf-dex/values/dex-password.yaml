config:
  enablePasswordDB: true
  staticPasswords:
  - email: "${email}"
    hash: "${hash}" # bcrypt hash of the password
    username: "${username}"
    userID: "08a8684b-db88-4b73-90a9-3cd1661f5466"
